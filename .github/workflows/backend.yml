name: Backend CI/CD - GCP

on:
  push:
    branches: [ main ]
    paths:
      - 'bknd/**'
      - '.github/workflows/backend.yml'

env:
  PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
  REGION: ${{ vars.GCP_REGION }}
  AR_REPO: ${{ vars.ARTIFACT_REGISTRY_REPO || 'stocks-backend' }}
  SERVICE: backend-service-java-2
  IMAGE_NAME: backend-java-2

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        shell: bash
        working-directory: bknd  

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Auth to GCP (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SA_EMAIL }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker to Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet
        working-directory: .

      - name: Give execute permissions to mvnw
        run: chmod +x mvnw

      - name: Build jar with Maven
        run: ./mvnw clean package -DskipTests

      - name: Build & Push Docker image
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          IMAGE_URI="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}"
          
          echo "🔨 Building Docker image..."
          docker build -t "$IMAGE_URI" .
          
          echo "📦 Pushing image..."
          docker push "$IMAGE_URI"
          
          DIGEST=$(gcloud artifacts docker images describe "$IMAGE_URI" --format='value(image_summary.digest)')
          echo "IMAGE_URI_DIGEST=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.IMAGE_NAME }}@${DIGEST}" >> $GITHUB_ENV

      - name: Clean up existing service (if conflicts exist)
        working-directory: .
        continue-on-error: true
        run: |
          echo "🧹 Checking if service needs cleanup..."
          gcloud run services delete "${{ env.SERVICE }}" \
            --region "${{ env.REGION }}" \
            --quiet || echo "Service doesn't exist or already clean"

      - name: Deploy to Cloud Run
        working-directory: .
        run: |
          gcloud run deploy "${{ env.SERVICE }}" \
            --region "${{ env.REGION }}" \
            --image "${IMAGE_URI_DIGEST}" \
            --platform managed \
            --allow-unauthenticated \
            --service-account "run-backend-sa@${{ env.PROJECT_ID }}.iam.gserviceaccount.com" \
            --add-cloudsql-instances "kodehaus-prod-474304:us-central1:pg-prod" \
            --memory 2Gi \
            --cpu 2 \
            --min-instances 0 \
            --max-instances 10 \
            --timeout 600 \
            --port 8080 \
            --startup-cpu-boost \
            --cpu-throttling \
            --no-cpu-throttling \
            --set-env-vars "SPRING_DATASOURCE_DB=postgres,SPRING_DATASOURCE_USERNAME=postgres,SPRING_DATASOURCE_PASSWORD=Pr0d-ChangeMe!,JWT_SECRET=mySecretKeyForJWTTokenGenerationAndValidationMustBeAtLeast512BitsLong12345678901234567890"

      - name: Verify deployment
        working-directory: .
        run: |
          SERVICE_URL=$(gcloud run services describe "${{ env.SERVICE }}" \
            --region "${{ env.REGION }}" \
            --format='value(status.url)')
          
          echo "🌐 Backend URL: ${SERVICE_URL}"
          
          # Esperar que el servicio esté listo
          echo "⏳ Esperando 45 segundos para que el servicio inicie..."
          sleep 45
          
          echo "🔍 Verificando health check..."
          for i in {1..10}; do
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${SERVICE_URL}/actuator/health" || echo "000")
            
            if [ "$HTTP_CODE" -eq 200 ]; then
              echo "✅ Backend health check passed!"
              exit 0
            else
              echo "⚠️ Intento $i: Health check returned HTTP ${HTTP_CODE}"
              if [ $i -lt 10 ]; then
                echo "   Reintentando en 10 segundos..."
                sleep 10
              fi
            fi
          done
          
          echo "❌ Health check failed after 10 attempts"
          echo "📊 Check logs at: https://console.cloud.google.com/run/detail/${{ env.REGION }}/${{ env.SERVICE }}/logs"
          exit 1

      - name: Display deployment info
        if: success()
        working-directory: .
        run: |
          SERVICE_URL=$(gcloud run services describe "${{ env.SERVICE }}" \
            --region "${{ env.REGION }}" \
            --format='value(status.url)')
          
          echo ""
          echo "🎉 =================================="
          echo "   Backend Deployed Successfully!"
          echo "   =================================="
          echo ""
          echo "🌐 Backend URL: ${SERVICE_URL}"
          echo "📦 Image: ${IMAGE_URI_DIGEST}"
          echo "🗄️ Database: postgres@kodehaus-prod-474304:us-central1:pg-prod"
          echo "📊 Logs: https://console.cloud.google.com/run/detail/${{ env.REGION }}/${{ env.SERVICE }}/logs"
          echo ""
          echo "🔗 Test endpoints:"
          echo "   Health: ${SERVICE_URL}/actuator/health"
          echo "   Login:  ${SERVICE_URL}/api/auth/login"
          echo ""
